<views:ConfigViewBase x:Class="SteadySight.Views.EdgeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:SteadySight.Views">
    <StackPanel>
        <TextBlock Text="边缘稳定" FontSize="26" FontWeight="SemiBold" Margin="0,0,0,6" />
        <TextBlock Text="在屏幕四周提供柔和参照，减少周边视觉刺激。"
                   FontSize="13" Foreground="{StaticResource App.TextSecondary}" Margin="0,0,0,20" />

        <GroupBox Header="样式">
            <StackPanel>
                <TextBlock Text="边缘样式" Margin="0,0,0,6" />
                <ComboBox x:Name="StyleCombo">
                    <ComboBoxItem Content="柔和遮罩（推荐）" Tag="SoftShade" />
                    <ComboBoxItem Content="四角取景框" Tag="CornerFrame" />
                    <ComboBoxItem Content="四边参考线" Tag="FullFrame" />
                    <ComboBoxItem Content="关闭" Tag="Off" />
                </ComboBox>
                <TextBlock Text="选择后立即生效，游戏中可按 F2 循环切换。" FontSize="12"
                           Foreground="{StaticResource App.TextSecondary}" Margin="0,8,0,0" />
            </StackPanel>
        </GroupBox>

        <GroupBox Header="强度">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="180" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="70" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="边缘宽度" VerticalAlignment="Center" />
                    <Slider x:Name="WidthSlider" Grid.Column="1" Minimum="4" Maximum="38"
                            TickFrequency="2" IsSnapToTickEnabled="True"
                            />
                    <TextBlock x:Name="WidthValue" Grid.Column="2" Text="14%"
                               VerticalAlignment="Center" HorizontalAlignment="Right" />
                </Grid>
                <Grid Margin="0,14,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="180" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="70" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="遮罩透明度" VerticalAlignment="Center" />
                    <Slider x:Name="OpacitySlider" Grid.Column="1" Minimum="5" Maximum="85"
                            TickFrequency="5" />
                    <TextBlock x:Name="OpacityValue" Grid.Column="2" Text="42%"
                               VerticalAlignment="Center" HorizontalAlignment="Right" />
                </Grid>
                <TextBlock Text="数值越高，缓解越强，但对画面遮挡越明显。建议从低到高逐步调整。"
                           FontSize="12" Foreground="{StaticResource App.TextSecondary}" Margin="0,12,0,0" />
            </StackPanel>
        </GroupBox>
    </StackPanel>
</views:ConfigViewBase>
